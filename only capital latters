#include <iostream>
#include <string.h>
#include <stdio.h>
using namespace std;

int main(int argc, string argv[])
{
    string s;
    getline(cin, s);
    for (int i=0; i<s.length(); i++) 
    {
        if (s[i]>65 && s[i]<92) printf("%c ", s[i]);
    }
    printf("\n");
}
